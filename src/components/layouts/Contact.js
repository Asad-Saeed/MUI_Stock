import React from "react";
import { Typography, Container } from "@mui/material";
const Contact = () => {
  return (
    <div>
      <Container sx={{ mt: "50px" }}>
        <Typography variant="h2">Hello Contact</Typography>
      </Container>
    </div>
  );
};

export default Contact;
