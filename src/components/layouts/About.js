import React from "react";
import { Typography, Container } from "@mui/material";
const About = () => {
  return (
    <div>
      <Container sx={{ mt: "50px" }}>
        <Typography variant="h2">Hello About</Typography>
      </Container>
    </div>
  );
};

export default About;

